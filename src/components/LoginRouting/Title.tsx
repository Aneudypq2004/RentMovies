
export default function Title({ title }: { title: String }) {
  return (
    <legend className="font-bold tracking-wide  uppercase my-4 text-xl text-azure-radiance-700 ">
      {title}
    </legend>
  )
}
